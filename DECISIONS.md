# DECISIONS.md — 自主决策日志（append-only）

> 格式：`日期 | 阶段 | 决策 | 理由 | 影响`
> 主窗口自主决策落此；你随时可回看/否决。被否决的也记（含新方向）。

---

## 2026-07-01 | P0 | 新建专用 `vtc` conda env（python 3.11 + torch cu128） | 三既有 env（fastv/qwen3vl/qwen3vl_clean）均缺 accelerate/pillow/mmcv 且绑别项目栈；base 待 P1 末定，复用会污染或被绑死；研究项目长期可复现性优先 | env 构建脚本 `scripts/build_env.sh`，~2.10/cu128；base 专属依赖（mmcv/flash-attn/qwen）延后 P1/P2 加 | 后台构建中

## 2026-07-01 | P0 | base 专属依赖（mmcv/flash-attn）不在 P0 安装 | base 尚未选定，mmcv 是 LLaVA 专用且编译重，flash-attn 编译耗时；P1 选定 base 后按需装 | P0 env 为"通用骨架"，P1/P2 再补 base 栈 | —

## 2026-07-01 | P0 | vtc 落地为 torch 2.11.0+cu128 / transformers **5.12.1**（比预期 4.x 新）；采用"双 env"策略 | cu128 index 当前发 torch 2.11；transformers 已进 5.x major，API 较 4.x 有变；既有 legacy `fastv` env（torch 2.0/transformers 4.31）保留用于在 LLaVA-1.5 上复现 FastV 原始基线 | 新方法与 Qwen2.5/3-VL 走 `vtc`；FastV@LLaVA-1.5 复现走 `fastv`；P1 文献定位时确认 transformers 5.x 下基座加载路径 | —

## 2026-07-01 | P1 | **选定 Gap A：serving-engine-aware 视觉 token 压缩**（部署级真实吞吐，集成进 vLLM 内部） | survey 23 法 0/23 在 serving engine 内测吞吐；Novelty5×Feasib4=20 最高；B/C/D 被 2025-26 SOTA 挤占；A 的对手是"空白"非强基线 | 方向锁定；详见 notes/positioning.md | —
## 2026-07-01 | P1 | 贡献框定为"方法 + 测量"，非纯测量 | 纯"塞 FastV 进 vLLM 量吞吐"易被审稿人判为工程 paper；故方法 = 与 serving 协同设计的压缩器（边界级 prune + 感知 KV-cache/batch），测量为支撑 | 决定 P2 方法设计取向 | —
## 2026-07-01 | P1 | 基座：LLaVA-1.5-7B 起步 → Qwen2.5-VL-7B-Instruct 泛化 | LLaVA-1.5 vLLM 支持最成熟、576 tok/img、可复用 FastV/SparseVLM/VisionZip ckpt→最快出结果；Qwen2.5-VL 的 M-RoPE/变长 token 是更难更可发的部署故事 | 双基座策略；先骨架后泛化 | —
## 2026-07-01 | P1 | **设 go/no-go 闸门为 P2 第一里程碑（claim-overturn 风险）**：LLaVA-1.5-7B@vLLM 边界级压缩器 {0,25,50,75}%，测 served tok/s/req/s/TTFT/KV-cache on GQA+TextVQA | 核心 claim 押在"压缩→真实 wall-clock 加速"，但 under continuous batching 极可能 wall-clock 几乎不动；重投入前必须先测 | GO(≥1.5×prefill & ≥1.2×e2e req/s @ ≤2%掉点)→设计方法；NO-GO(<1.2×e2e)→转 negative-result paper 或 Gap D；NO-GO 触发 charter §6 升级找人 | 项目最大风险点
## 2026-07-01 | P1 | 设 novelty 复核闸门（probe 前先跑） | survey 截止 2026-07-01，但领域迭代快，"0/23"是核心 novelty，若有竞品 landed 需重估 | Dev 在 P2 step-1 做 last-6mo 复扫 | —

## 2026-07-01 | P2-step1 | **三 env 策略落地：vtc / vtc_serve / fastv** | serving 必须用 vLLM，但 vLLM 与 vtc(torch2.11+cu128/tf5.12) 不完全兼容；FastV 复现需 legacy 栈。分 env 隔离依赖污染 | `vtc`（方法开发/CPU test，torch2.11+cu128/tf5.12.1）；`vtc_serve`（vLLM serving，**vLLM 0.10.2 + torch 2.8.0+cu128 + transformers 4.55.2 + numpy 2.2.6 + numba 0.61.2**）；`fastv`（legacy，torch2.0.1+cu118/tf4.31，复现 FastV 精度基线） | vtc_serve 已通过 1-image 烟测（runs/vllm_smoke.log，2.4s/答） |
## 2026-07-01 | P2-step1 | **vtc_serve 锁定 vLLM 0.10.2（非最新 0.24）** | vLLM 0.24 预编译 wheel 绑 CUDA 13（vllm._C_stable_libtorch 需 libcudart.so.13），本机 driver 560 仅支持 cu12.x；0.24 在 driver 560 上 EngineCore init 直接挂。0.10.2 是最后一个 cu12-native 大版本，torch2.8+cu128 在 driver 560 跑通。tf 必须 ≤4.x（vLLM 0.10.2 的 get_cached_tokenizer 调 all_special_tokens_extended，tf 5.x 已删该属性）→ 锁 tf 4.55.2（vLLM 0.10.2 的 floor，且保留该属性）。numpy 必须 ≤2.2（numba 0.61.2 上限）→ 锁 2.2.6 | vLLM 0.10.2 的 multimodal processor path 与 method-design.md §1b 描述一致（已核实源码 llava.py:96/649/660），hook 方案无需改 | 升级 driver 或换卡前不升级 vtc_serve 的 vLLM |
## 2026-07-01 | P2-step1 | **novelty 复核通过：Gap A 仍 OPEN，无 blocker** | 2026-H1 复扫：ElasticMM(2507.10069) 是 scheduling/disaggregation 非 compressor 且明确拒做压缩；survey 2507.20198 v5 独立证实 gap 存在并诊断 FlashAttention 不可得根因；EffiVLM-BENCH(2506.00479) 仅离线 HF latency、不入 engine；AgilePruner/VisionTrim/PRUNESID 挤占 accuracy/FLOPs 组合研究空间但不碰 serving-throughput；vLLM RFC#45098 仍是未完工 infra | differentiator 必须守住"serving-engine 真实吞吐"，不滑向又一篇 accuracy/FLOPs 组合研究 | 无 blocker，proceed to go/no-go probe |

## 2026-07-01 | P2 GATE | **go/no-go 判定：provisional GO（核心 claim 成立，非 NO-GO，不触发 §6 升级）** | GQA@r50 (n=200, 连续 compaction 288/576)：e2e req/s 1.77→2.35 = **1.33×**（远 > 1.2× NO-GO 线 ⇒ "压缩在 serving engine 下有真实 wall-clock 加速" **确认**，survey 担心的 ~0 加速被证伪）；prefill/TTFT 631→510ms = 1.24×（< 1.5× 目标，因 pruning 在 projector 输出、vision tower 仍跑全 576 token、编码器开销固定）；accuracy 暂不可读（scorer 太严 + proxy selector） | **provisional GO**：proceed 跑全曲线(r25/r75/TextVQA) + 进方法设计（真 CLS-attn selector + serving-aware：更早 prune 提速编码器、KV-cache 感知） | 两条论文级发现：(1) **e2e 加速(1.33×) > prefill(1.24×)** → 压缩的部署收益主要来自 KV-cache/并发（每请求 KV 更小→更高并发→更高 req/s），而非 prefill FLOPs——此 serving 专属效应离线 FLOPs 测不到，是核心差异点；(2) prefill 次线性 → 编码器固定开销，启示编码器内/后更早 prune。准确判据待 scorer 修好重打分 + 真 selector | —

## 2026-07-01 | P2 | **v1 真 CLS-attn selector 失败（OCR 灾难）→ 方法重定向到 query-aware boundary selector** | 三方对比(proxy=hidden-state / v1=边界真CLS / FastV=intra-LLM 注意力秩)：GQA r25 v1 0.570 > proxy 0.555，但 r75 v1 0.450 < proxy 0.470；**TextVQA v1 灾难**（r50 0.445 vs proxy 0.530，r75 0.275 vs proxy 0.440）——vision-tower CLS 注意力**不关注文本/OCR 区域**，高压缩丢光 text patch。FastV(intra-LLM、基于任务 token 的注意力秩) 在 GQA r75(0.515) 与 TextVQA r50(0.555) 均占优。真CLS capture 另加 ~5-8% 吞吐开销（每 request 手算 softmax）| **selector 必须是 query/task-aware（用问题文本选视觉 token），非视觉显著性**；且须 vLLM 可集成（边界 prune，intra-LLM 在 vLLM 不可行——V1 子进程+CUDA graph 锁 decode）| **方法 v2 方向**：边界 query-aware selector（问题文本→patch 相关性，vLLM 预处理时可得）+ 早 prune（编码器开销杠杆）+ KV-cache 感知；served 吞吐为差异判据。provisional GO 不变（serving 加速由 proxy probe 确立，与 selector 无关）| selector 设计是下一 Dev 任务

## 2026-07-02 | P2 | **v2(LLM-embed cosine query) 也失败 → 根因=嵌入空间不对齐 → A'' 用 CLIP 对比对齐；方向定 A''→D，基座不换** | v2 TextVQA r50 ~0.38 < proxy 0.50。研究 agent 诊断：v2 用 LLM embed_tokens(词义)×post-projector patch(LLM 空间)，**两者非对比训练、cosine 对不上字形**。SparseVLM 是 intra-LLM（不可迁移），但其 text↔patch 信号可用 **CLIP text encoder × CLIP ViT patch 特征**（对比训练、对齐）在边界 training-free 复现 → OCR/text patch 得分高。**A'' = 边界 CLIP 对比 selector**（v2 的正确修法）。Qwen3.5-VL 不存在；真实 Qwen3-VL(2511.21631) 8B 装得下 A40(bf16 ~16GB)，32B 不可行——**先 LLaVA-1.5-7B 落方法，Qwen3-VL-8B 作泛化行**（不重置）。served-throughput gap 仍开放 | **执行 A'' 先（验证 CLIP 对比能否破边界 OCR，目标达 FastV 级 TextVQA），通过则折入 D（vLLM 集成 + 早 prune/pre-projector 选择 + 负载自适应 budget）**。定位"首个 vLLM 集成、保 OCR 的 training-free 压缩"。详见 notes/new-papers-and-qwen35vl.md | A'' selector 实现是下一 Dev 任务；A'' + 早 prune 协同（pre-projector 选择既 query-aware 又省编码器开销）

## 2026-07-02 | P2 | **A''(CLIP 对比 boundary selector) 也失败 → 三连败，停止 selector 追逐，pivot 到 D-on-proxy** | A'' TextVQA r50 n=50 = 0.180（< v2 0.38 < proxy 0.50）。**实证根因**：CLIP 对比损失只对齐 pooled [CLS]、**不对齐 per-patch 特征**（合成 "STOP 123" 测：per-patch projection/CLS-attn/attention-rollout/MaskCLIP 全 0/10 命中文本区域）= MaskCLIP/CLIP-Surgery 已知结果。三个 boundary TF selector（CLS-attn 0.445→LLM-cosine 0.38→CLIP 0.18）全败，proxy(hidden-state) 0.530 仍是边界最好、FastV(intra-LLM) 0.555 最准但不可集成 | **边界 TF 廉价信号打不过 intra-LLM OCR——这是结构性的，文献 5+ OCR 方法因而用训练/学习组件**。training-free 约束下接受 proxy 级精度 | **pivot 到 D-on-proxy**：方法 = proxy selector + serving-aware 优化（早 prune 若编码器开销占比值得 + **负载自适应/KV-cache 感知 budget**，finding #1 的 serving 专属新意）。论文脊梁 = **served-throughput 测量（0/37）+ 3 条 serving 发现**；方法贡献 = serving-aware budgeting（非 selector）。provisional GO 不变。详见 eval/p2_method_a2_findings.md | 下一：先测 prefill 拆解（vision tower vs LLM 占比，定早 prune 值不值得）+ 测 并发×prune-rate 权衡（验负载自适应 budget 概念），再定 D 实现范围

## 2026-07-02 | P2 | **D 范围测量锁定：BUILD 负载自适应 budget，SKIP 早 prune** | M1：vision tower 仅占 **6.6% TTFT**（12.7ms/180ms LLM prefill）→ 早 prune/mid-encoder 手术不值得（<10%），赢面全在 LLM 序列缩短（proxy 已拿）。M2（GQA, 并发×prune）：c1 时 r50/r75 = 1.17×/1.26×；**c12 时 r50/r75 = 1.42×/1.75×** —— 加速随并发增长（r75 +0.49）→ **负载自适应 budget 强烈成立**（KV-pressure 释放随并发+剪枝深度放大，0/37 碰过的杠杆）。headline 数字：c12/r75 = **1.75× req/s** | **D = 负载自适应/KV-cache 感知 budget**：r = f(并发, KV-占用) ∈ [r_min, r_max]，高负载多剪（抬 req/s）、低负载少剪（保精度）；KEEP proxy selector + served-throughput 评测（并发×prune 矩阵作 headline）；accuracy guardrail r_max ≤ 0.50（r75 掉点 ~11%）。SKIP 早 prune | D 实现是下一 Dev 任务；详见 notes/p2_d_measurements.md | —

## 2026-07-02 | P3 | **D 验证（P3-step-1）：load-adaptive 收益是 benchmark-conditional；num_running 控制器；修 gap-block bug** | 控制器改 num_running/max_num_seqs 信号（KV-occ 在 c12/短序列峰值仅 ~0.04→r barely 离开 r_min），conc_lo=0.25/conc_hi=0.75 → realized r 真在 [0.25,0.50] 全范围跑（bursty 交替 2/12 burst）。修了 gap-block 重复行 bug（200→249 污染 acc）。**mt32 干净 Pareto（n=200）**：TextVQA adaptive 干净 Pareto-dominate 两固定点（req/s +0.044>r25, acc +0.020>r50）；**GQA adaptive 不占优**——mt32 下 r50 acc-中性（短 yes/no 可恢复，r50 0.565≥r25 0.550）→ fixed-r50 双轴占优，prior mt16 的 GQA Pareto 胜是截断解码假象（r50 acc 被压到 0.522）| **load-adaptive 的收益 = benchmark-conditional：压缩有 acc 代价处（OCR/密集文本/长答案）才有收益；模型可恢复处（短答案）直接 fixed-r_max**。这是更干净的 framing | **P3-step-2 验证模式**：扩到 MME/MMBench/ScienceQA（长答案/密集文本→r50 应有 acc 代价→adaptive 应胜）；若 adaptive 在多数 costly-compression 任务上 Pareto-dominate，故事强；FastV anchor + 并发矩阵 + 消融 + n=500 TextVQA 收紧 | —

## 2026-07-02 | P3 | **P3-step-2 广度验证 + ⚠️ TextVQA n=500 反转头条：方法 framing 改为"throughput-optimal under guardrail"** | mt64 n=200：**MME** adaptive Pareto-dominate（req/s 2.98>r25 2.94, acc 0.700>r50 0.685）；**ScienceQA** adaptive Pareto-dominate（3.01>2.94, 0.685>0.670）；MMBench r50 占优（r50 acc-中性）。**TextVQA n=500 反转**：n=200 的 adaptive>r50 +0.020 acc 是噪声，n=500 adaptive 0.510 < r50 0.526（符号翻转，不显著）→ TextVQA "Pareto-dominate" 降级为仅 req/s。FastV anchor（in-LLM layer-2）accuracy 可比 proxy r50 但跑不进 vLLM。并发矩阵确认 M2（c1→c12 r50/r0 1.17×→1.42×）| **robust 信号：adaptive 在全部 5 基准 req/s 胜 r25（+2-7%）；acc 胜 r50 仅在 r50 有 acc 代价的基准（MME、ScienceQA = 2/5）**。判别子 = per-benchmark r50-acc-cost，非答案密度（MMBench 密集 MC 但 r50 免费）| **方法 framing 改为"throughput-optimal under per-benchmark acc guardrail"**（adaptive 比 fixed-r25 高吞吐、在 acc-sensitive 任务上避开 r50 的 acc 代价）—— 非"Pareto-dominant"。**论文脊梁重定为 measurement-led**：served-throughput 测量（0/37）+ 3 发现是主贡献；load-adaptive 方法是 supporting（real 但 modest/conditional）。详见 notes/p3s2_pareto.md | —

## 2026-07-02 | P3 | **⚠️ P3-step-3 n=500 收紧推翻方法 Pareto claim（0/5）；方法降为"dominate r25 on 3/5"。触发 §6 升级找人** | n=500（c12/bursty/num_running）：MME 与 ScienceQA 的 n=200 Pareto-dominate **均反转**（acc 边际 z<0.6，噪声）；GQA adaptive 与 r25 平手（req/s -0.006, acc -0.006）。**clean Pareto-dominate @ n=500 = 0/5**。连"req/s 胜 r25 everywhere"也弱化：n=500 下 adaptive 仅在 MMBench(+0.053)/ScienceQA(+0.052)/TextVQA(+0.037) req/s 胜 r25，GQA/MME 平手。**adaptive 无法在 acc 上胜 r50 任何基准** | **方法仅剩诚实 claim：在 3/5 基准 Pareto-dominate fixed-r25（iso-r25-acc 下更高 req/s）= "以 r25 精度为地板时白送吞吐"**；非双轴 Pareto，modest/conditional。1-D load-adaptive（prune rate vs concurrency）打不过 well-chosen fixed rate | **测量支柱不受影响**（0/37 + 3 发现 + 1.75×/并发放大依然 solid）。**升级找人**：方法支柱虚，需定论文走向（measurement-led + 诚实 modest 方法 / 加强方法 / 转方法角度）。详见 notes/p3s3_pareto_n500.md | —

## 2026-07-02 | P2 | **D 实现完成：负载自适应 budget 可工作，calibrate occ 阈值到引擎实际负载区间** | 引擎负载读取在 vLLM V0 **可工作**：`llm.llm_engine.scheduler[0].running`（运行序列数）+ `.block_manager.get_num_free_gpu_blocks()/.num_total_gpu_blocks`（KV-占用）—— 清晰可读，**无需 fallback**。两处实现坑：① sync `llm.chat()` 会排空引擎，控制器在 segment 边界读到的永远是空引擎 → 改用 **engine-level streaming loop（add_request + step）**，segment 内逐请求 add 并在 add 间 step() 推进调度器，使后续请求的负载读取能看到前序请求已占的 KV（实测 num_running 0→12、occ 0→0.11 单调上升）。② c12/短序列下 KV 池(3085 blocks)远大于 12 并发所需(~120 blocks)，峰值 occ 仅 **0.11** → 默认阈值 0.40/0.70 永不触发 → **calibrate 到 occ_lo=0.02/occ_hi=0.10**（部署区间的合理调参）。smoke 实测 r 从 0.25 升到 0.274+（hook #4 kept=418）证明控制器在自适应。 | **occ 阈值必须匹配部署负载区间**（不是写死 0.4/0.7）—— 这是合法调参，不是作弊；论文报阈值 + 实测 occ 分布。streaming-loop 是展示在线控制器行为的唯一可靠方式（sync API 看不到 mid-flight 负载） | D 验证矩阵（adaptive vs fixed-r25/r50 under bursty）跑 n=100 快验；n=200 + TextVQA + step profile 队列在 notes/d_method_jobs.json 给 Main | 详见 notes/p2_d_results.md（跑完后填表）+ src/load_controller.py + src/serve_bench.py（--adaptive/--load-profile）

## 2026-07-03 | P3 | **B 路径（KV-admission）尝试后放弃（user-killed）；回 A（measurement-led 写作）** | P3-step-4 在 1×A40 上调 KV-bound regime：peak KV-occ 仅 0.148（需 >>0.5 才算 KV-bound），46GB KV pool 太大、可用并发压不出强 KV 压力 → thesis（load-adaptive 收益 KV-bound 下浮现）在本地硬件上**未能验证**（被 user 中途停掉，未到 adaptive-vs-fixed GO/NO-GO）。这正是之前标注的"1×A40 KV pool 太大可能压不出 KV-bound"风险。partial 产物（serve_bench load-trace + p3s4 regime 探针）已提交作记录 | **B 放弃，回 A**：measurement-led 写作。方法部分如实写：1-D load-adaptive n=500 仅 dominate r25 on 3/5（modest）；KV-admission 探索作为 future work / 局限（需 KV-bound 硬件或多卡）。**测量支柱（0/37 + 3 发现）是论文主轴，不受影响** | 启动 P4 写作 | —

## 2026-07-03 | v2 转向 | **v1 论文致命弱点（user 批评）→ 重做实验核心：V1 引擎 + 现代模型 + 真实 serving 规模** | v1 问题：① 方法 n=500 后归零；② 测量发现对系统人不意外；③ **单模型+过时模型(LLaVA-1.5)+已废弃引擎路径(V0)** 最致命；④ serving 规模不够格（无 c≥64/p50/p99/goodput）；⑤ "0/37" 负向声明脆弱。user next_plan 指令：P0 迁 vLLM V1（V0 已废弃，V1 prefill/decode 分离+chunked prefill 影响 F1，须复测；V1 模型在独立进程→§4.3 in-process scheduler 读取失效→controller 改走 metrics/API；迁移本身写成新工程贡献节）；P1 加现代模型（Qwen3-VL-8B 动态 token，验证 F1/F2/F3 是否仍成立）；P2 并发扫到 c≥64 + p50/p99 TTFT + goodput；P3 接 encoder-side 压缩器（VisionZip 类）横向对比；每步前 web 核实版本+novelty-threat 监控。新增相关工作 DeepSeek-OCR(Contexts Optical Compression)、vLLM-Omni(2602.02204) | paper_v1 降为待大改的草稿（不投）；先核 V1 硬件可行性（0.24 绑 CUDA13 挂过→V1 可能同样问题）→ 这是 v2 命门 | 详见 next_plan.md | —

## 2026-07-03 | v2 P1 | **Qwen3-VL-8B 泛化：F1/F2/F3 部分成立 → served-speedup 是 architecture-conditional** | Qwen3-VL-8B@V1：F1 衰减（c12/r75 1.29× vs LLaVA-1.5 1.86×，并发 bonus +0.21 vs +0.65）；F2 vision+merger+deepstack 仅占 10% TTFT（merger 高效，非重固定开销）；F3 成立（TextVQA 748tok r50 1.16× > GQA 279tok 1.06×，动态分辨率给密集文本更多 token）。**关键：原生 2×2 merger 与 post-hoc pruner 是替代（都压 post-merger token）→ 边际递减**；价值 workload-conditional（已压缩 object QA 低、文本密集但 acc 脆）| **论文 reframing**：served-speedup 是 architecture-conditional（固定 token 旧模型强、原生压缩现代模型弱），机制=native compression 替代 post-hoc pruning。比"处处成立"更诚实可发。暗示未来方法：pre-merger prune（让 pruner 与 merger 不竞争）| 测量现跨 2 引擎(V0/V1)×2 架构(LLaVA-1.5/Qwen3-VL)，回应"单旧模型+废弃引擎"批评。详见 notes/v2_p1_qwen3vl.md | —

## 2026-07-03 | v2 P2 | **真实 serving 规模：c64 + goodput Pareto，压缩"抬高硬件吞吐天花板"** | 1×A40 c64 是 honest 天花板（r0 KV-bound 41GB≈budget；c128 不可行）。r75/r0 = 1.19×(c1)→1.53×(c4)→1.96×(c16)→**2.22×(c64)**——放大未饱和、渐近；r0 在 c16→c64 plateau 而 r75 继续爬→**压缩抬高硬件吞吐天花板**。p99-TTFT c64 r0 18.4s→r75 6.5s（2.84× 更低，剪在 queue/tail 不在 floor）。**Goodput Pareto（部署 win）**：TTFT≤5s SLO r75 13.7 vs r0 1.8 req/s(7.4×)；**r75 @c64 严格 dominate r0（2.22× 吞吐 + 2.84× 更低 p99，无 tradeoff 纯赚）**。Qwen3-VL c64 r50/r0 1.34×（c12 后仍升，方向一致衰减）| 拆 v1"规模不够格"批评——c64+p50/p99+goodput 全补齐；goodput Pareto 是比裸 req/s 更强、更部署相关的 headline | 详见 notes/v2_p2_scale.md。下一步 P3 横向压缩器 | —

## 2026-07-03 | v2 P3 | **跨压缩器：served-throughput win 是 FRAMEWORK 属性（4/4 通用），prune-vs-merge tradeoff 确认** | c64 × {r0,r50,r75} 跑 proxy/true_cls(CLS-family)/tome_merge(ToMe)/random：**每个压缩器 r75 都严格 dominate r0**（2.18-2.30× 吞吐 + 2.66-2.85× 更低 p99-TTFT）；**throughput 在 iso-k 下与 selector 无关**（r75 req/s 19.7-20.7）→ served-throughput win = framework 属性（placeholder-shrink→真实 KV 释放），非 proxy artifact。**直接拆"只 proxy"+"0/37 脆弱"批评**。**prune-vs-merge**：ToMe(merge) r75 acc +0.065 vs proxy（0.540 vs 0.475，补回 55% 掉点）仅 −1.5% 吞吐；merge 保信息优势随 r 增长。诚实 red flag：random r75(0.535)>saliency(proxy 0.475/cls 0.490)——GQA FastV 失败模式，推 query-aware | v2 实验阶段(P0-P3)完成：2 引擎×2 架构×4 压缩器×c64+goodput，全 v1 致命伤已系统拆解 | 详见 notes/v2_p3_crosscompressor.md。下一步 P4 全文重写 | —

## 2026-07-03 | 方法转向 | **pivot 到 ElasticVis/TokenSched：每请求视觉 token 预算分配作 serving 方法** | 全部 37 法（含 v2 控制器）用全局剪枝率；ElasticVis 让引擎在准入时**逐请求**分配 k_i（按负载/SLO 余量/请求特征），目标 **goodput@SLO**，形式化在线分配 `max Σ accuracy(k_i) s.t. LatencyPred(Σk_i,load)≤SLO`。0/37 做逐请求预算→新意干净。绕开饱和的"选哪些 token"（selector 三连败+proxy 天花板+native merger 替代），换成"每请求给多少"=scheduling 问题，引擎有独特杠杆。accuracy-k + latency(k,load) 曲线在 v2 probe 数据现成→这是在**已测输入上的分配/优化问题**，非新测量 | **v2 测量 framework 是 ElasticVis 的 substrate**（不浪费）：framework=eval+objective、accuracy(k)=目标项、latency(k,load)=约束预测器、V1+c64+goodput=harness。v2 论文仍可独立投稿（measurement-led fallback） | 新主对话（干净上下文）执行；先读 `notes/elasticvis_positioning.md` + STATE | —

## 2026-07-07 | ElasticVis EV-1d | **✅ GPU open-loop 赢了（+7.5% under SLO 压力）→ 方向成立；robust placeholder-shrink 集成；magnitude 偏小且 allocator 待重校准** | EV-1d 决定性 GPU 测试（user 批准 A）：serve_bench 加 open-loop Poisson 到达（--arrival openloop --arrival-rate R）。**结果**：rate=8 轻载 EV 0.75× 输（无 SLO 压力）；**rate=15 近饱和 EV 2.739 vs r0 2.549 = +7.5% WIN**（SLO 压力激活机制：EV 给紧 deadline 请求 k144 释放容量→多满足 SLO）。**关键工程成果**：agent 换成 **placeholder-shrink 集成**（get_num_image_tokens 设 k_i，projector 直通，vLLM 原生 gather_mm_embeddings 切 [0:k_i]）→ 不用 embed_multimodal/rid patch，**c64/limit=200 不崩**（解决 EV-1c 批 hook 崩溃）。per-row-k 差异化 200/200 稳定。**caveat**：+7.5% 是 allocator 用**闭环 c64 饱和延迟常数**跑的，open-loop 实际 compute ~10× 低→ allocator 高估延迟→轻载白给 k144 掉精度（rate=8 输因）。TextVQA acc range 0.28 限 headroom | 下一步：重校准 allocator（load-dependent gate，用 live num_running 估延迟）拿公平 magnitude；更陡 benchmark（DocVQA/OCR range>0.4）作 EV-2 提升headroom | —

## 2026-07-07 | ElasticVis EV-1c GPU | **⚠️ closed-loop GPU ElasticVis 输 -36%；sim mixed-k 队列模型不可靠；win 仅在 open-loop（未 GPU 测）= claim-at-risk** | n=80 apples-to-apples GPU（TextVQA c64, mixed-SLO 5000/15000）: fixed_r0.75 goodput_acc=**4.158**(best) vs ElasticVis=**2.663** → **ratio 0.640 输**。机制：closed-loop batch-submit 下 ElasticVis 给 slack 请求 k576（slot 8.4s）→ 高 k **阻塞队列** → 紧 deadline 请求排在后面错过 5s SLO → met_rate 暴跌(4.75 vs r0.75 的 13.31)。Greedy allocator 近视（只看 per-request SLO，不看高 k 阻塞他人的外部性）。**sim 预测 closed-loop +14.6% 赢 → 与 GPU 矛盾 → sim 的 mixed-k 队列模型不可靠**（slot 模型误模 mixed-k 排空/阻塞）。sim open-loop 预测 +34.8% 但因 sim 不可靠而存疑。**win 仅在 open-loop（项目原定 primary regime），但 serve_bench 无 open-loop 到达模式 → 未 GPU 测**；open-loop 更低并发可能也避开 limit=200 崩（placeholder race）。EV-1c-fix(rid 映射)使 per-request k 在 c64 工作（limit=80 验证），但 limit=200 仍崩（get_num_image_tokens 无 rid → 共享 cur_k race）| **升级找人**：A 加 open-loop 到达 GPU 测（decisive，测 primary regime）｜B 接受 regime 刻画（"open-loop 赢/closed-loop 输"+门控，弱）｜C 放弃 ElasticVis 回 v2 测量论文 | —

## 2026-07-07 | ElasticVis EV-1c | **ElasticVis c64 GPU 崩（FIFO desync）；fixed-r c64 可跑；SLO 阈值需调** | EV-1c probe：ElasticVis @c64 limit128 → hooks 正确触发 3 次（per-row-k 交替）后 **CUDA masked_scatter size mismatch (totalElements<=srcSize)** 崩；c16 也崩（小批 B=2,3 仍崩）→ **非并发规模问题，是连续批处理+chunked prefill 下 forward 图像顺序≠提交顺序的 fundamental desync**（k_fifo pop 给错行 → placeholder 数 ≠ gather embedding 数）。**fixed-r75 @c64 正常**：acc@k144=0.273（与 inventory 0.275 吻合✓），e2e p50=5090ms，goodput_at_slo 正常上报。SLO 阈值：3500ms 太紧（k144 e2e~5s→0/64 meet），需 ~6000/15000 才激活 H1b（低k meet紧SLO、高k meet松SLO）；per-request trace 可 post-hoc 重算任意 SLO 免重跑。**结论**：fixed-r baselines + accuracy(k) + 延迟模型 GPU 可测（校准 sim）；ElasticVis 本身 c64 需 robust row→rid 映射（vLLM mm-metadata 或图像编码 k 的 order-independent 方案），可能要 fork | 双线：调研 robust fix + 收 fixed-r baselines r0/r50 校准 sim；若 fix 可行→全 GPU ElasticVis，否则→sim 校准于 GPU baselines + 小规模机制证 | —

## 2026-07-07 | ElasticVis EV-1a | **per-request k 在 live vLLM 连续批处理 GPU 验证通过** | EV-1a 集成 serve_bench.py：per-row gather in batched projector hook（返回 per-image 不同 k_i 的 list；vLLM 0.19.0 V1 源码验证 sanity_check_mm_encoder_outputs/extend/_merge_multimodal_embeddings 支持 list），additive `--k-policy {fixed,segment,elasticvis}`，queue-aware greedy gate（expected_wait+P(k)≤slo），live load via get_metrics。**GPU smoke PASS**：3 图同批 per-row-k=[576,144,576]（2 distinct k in one batch）→ "同批共享 k"约束打破。**真 allocator 测试**（8 图 mixed-SLO 3500/15000ms）：per-row-k=[144,576,144,576,...] → **按 deadline 选 k（紧→144 松→576），H1b 机制在真实引擎工作**。caveat：enforce_eager=True 必须（CUDA graph 抓不住 dynamic list 返回）→ 吞吐为 eager-mode 下界，但相对比较(ElasticVis vs fixed)不受影响；k_fifo→batch 假设 FIFO 调度。**EV-1b 缺口**：goodput@SLO 指标未计算/上报（metrics JSON goodput={} slo_ms=None）→ 需补 per-request-deadline goodput@SLO，且须对任意 policy 都报（fixed-r 基线也要）| 下一步 EV-1b 补指标 → EV-1c TextVQA 跑 ElasticVis vs fixed-{r} goodput@SLO | —

## 2026-07-06 | ElasticVis EV-0 GO | **门控刻画验证 + TextVQA H1b +35.5% WIN（零 GPU sim）** | inventory 显示 TextVQA accuracy(k) 够陡（LLaVA true_cls 0.555→0.445→0.275 range 0.28；Qwen3-VL 0.77→0.48 range 0.29），远超 H1b crossover。门控预测被 5 benchmark 交叉验证：知识型(MME/MMBench/ScienceQA)~0.01→无 win；GQA 0.12-0.13→边界 NO-GO；TextVQA 0.28-0.29→WIN。synthetic sweep（`gating_sweep.py`）：H1b mixed-SLO crossover acc-range≈0.15，H1 uniform-SLO≈0.40（故混合-SLO 是稳健 regime）。**真实 TextVQA sim 确认**（`confirm_textvqa.py`）：H1b mixed-SLO(poisson8, 50%tight3.5s/50%slack15s, e2e) Greedy=2.36 vs bestFixed=1.74 = **+35.5% WIN**；H1 uniform-SLO=0.898 lose；GQA H1b=0.978 lose（边界）。机制=紧 deadline 给低 k 保吞吐/SLO、松给高 k 拿精度。**caveat**：sim slot 过串行(sanity k144 低估 2.5×，偏差利于 ElasticVis)，+35.5% 是相对比较(稳健)但 magnitude 需 GPU 确认 | **paper spine 定**：(1)门控刻画(acc-陡度 gate, 5 benchmark+sweep 验证) (2)ElasticVis 在 TextVQA H1b +35.5% 方法 win (3)v2 framework 作 substrate；GQA NO-GO=flat-acc 边界(illustrating the gate)；混合-SLO/deadline-heterogeneous 是真实多租户场景。下一步 EV-1 GPU：serve_bench 加 open-loop+混合-SLO+per-request k plumbing，实测确认 | —

## 2026-07-06 | ElasticVis EV-0 go/no-go | **OFFLINE NO-GO on GQA：ElasticVis≯fixed-r @goodput@SLO（real acc 0.476-0.595 太平）；Z 的 H1b '1.10× win' 是 MockAcc artifact** | EV-0 三组件全建成：predictors（LatPred LOO R²0.996、hybrid 修 c1 71s→433ms bug、accuracy 曲线+逐图翻转 46/200）、allocator（Greedy/Lagrangian/Fixed/Oracle，H1 机制本地验证：轻载 576/重载 144）、sim（slot+queue server，S(k)=64/served_req_s，closed_loop sanity order 对但 k144 met_rate 偏低 2.5×=slot 过串行）。**独立 probe**（`runs/elasticvis_ev0/probe.py`，real vs wide acc）：H1 均匀-SLO real-acc=0.79 LOSE / wide-acc(0.30-0.70)=0.94 仍LOSE；H1b 混合-SLO real-acc=0.998 TIE-LOSE（**非** Z 报的 1.10）/ wide-acc=1.307 WIN。**机制真实但被 accuracy(k) 陡度门控**：flat acc→goodput 吞吐主导→Fixed 低-k 赢；steep acc→精度主导→ElasticVis 赢。sim 的 k144 低估偏差**有利于** ElasticVis（低估 Fixed 低-k goodput、高估 high-k）→ GPU 实测只可能更差 → **NO-GO on GQA 稳健**。关键数据洞察（写进 design §2.1）：v2 ttft=compute(~2-3s)+segment-queue，k 的杠杆是吞吐(KV)非 compute | **升级找人定方向**：(A)探 steep-acc workload(TextVQA/OCR/doc-VQA)找 ElasticVis 的赢家 regime｜(B)GPU 确认 GQA NO-GO（~30min 定论）｜(C)重定位为"per-request 分配被 acc(k) 陡度门控"刻画贡献，并入 v2 测量论文｜(D)放弃 ElasticVis 回 v2 论文（已可投）| —

## 2026-07-06 | ElasticVis EV-0 | **novelty 扫描推翻"0/N per-request 预算"claim → 重定位为"系统信号驱动 allocator"；spine A→B、评测 open-loop+混合SLO 经 user 批准** | 三 scout（数据/代码/novelty）：① novelty——CARES(ACL'26,per-image 视觉 token 数=分辨率选择,**内容驱动无系统信号**)、DyToS(budget=单一用户延迟目标,非准入时逐请求)、PARCEL(字面"elastic visual-token compression",**多预算单模型=substrate 非竞品**)、AdaServe/SLOs-Serve/JITServe(per-request budget-for-goodput@SLO 但**文本域**) → "0/N"**不成立**；干净单元=**准入时、负载+SLO 裕度驱动、goodput@SLO 的 per-request 视觉 token 分配**。② 数据——逐请求延迟 15-cell 齐全(够 LatPred)、逐图 accuracy(k) 可恢复(53/200 翻转,但仅 3 k 点且无特征→H2 需新 probe)、Qwen3-VL 仅 req/s。③ 代码——集成 MODERATE,瓶颈是批 hook 的 rid↔行映射,逃路=剪枝前移到 per-request 预处理 | **spine A→B**(user)：核心 H1 系统信号 allocator(现有数据够,最快 headline),内容 H2 作后续扩展(对标 CARES)。**评测 open-loop 变载为主+混合-SLO 为辅**(user)：现有 c64 闭环准入负载≈常数(v2 逐段控制器 n=500 null 之因)→不是正确评测。**零 GPU offline-first go/no-go**：先建 `src/elasticvis/`{predictors,allocator,sim} 用离散事件仿真 replay 实测数据证/伪"ElasticVis>best fixed-r @goodput@SLO under open-loop",再花 GPU 实测确认。详见 `notes/elasticvis_design.md` | —

## 2026-07-07 | ElasticVis EV-1e 终判 | **❌ 公平 GPU 测试 ElasticVis 输（-0.9%~-24%）；根因=fundamental batch-level compute interference；sim 独立 slot 模型错；claim-overturn 最终判决** | EV-1e 重校准 allocator（load-dependent gate: compute(k)=400+1.14k ms, wait(nr)=7565·(nr/max)^1.82 + live EMA + SLO-aware caps）+ engine 公平修（EV 与 fixed-r 同设置，去掉 EV 专属 enforce_eager）。**公平 GPU open-loop TextVQA c64 mixed-SLO 结果**：rate8 EV 0.991×(tied)、rate12 0.885×输、rate15 0.757×输。rate15 早先 +7.5% 是不公平设置(enforce_eager 不对称+未校准)的 artifact。**根因(fundamental)**：continuous batching 同一 forward 的请求共享 GPU compute，k576 长 prefill 延迟整批 decode（含紧 deadline 请求）→ 给 slack 高 k 拖慢整批→紧请求 miss SLO；fixed-r50(k288) uniform cost 占 Pareto 最优。sim 独立 slot 模型不捕获 batch 干扰→+35.5% 不迁移。**结论：per-request 视觉 token 预算在连续批处理下不提升 goodput@SLO（架构性）**，H1b 前提(slack 高 k 不伤 tight)在真实引擎不成立 | 收尾方向待 user 定：A 折入 v2 论文 negative-finding 节｜B 独立 negative-result 论文｜C pivot SLO-aware batching(按 deadline 分批避免干扰) | —

## 2026-07-13 | EV-VAR pivot（user 指令）| **停止 ElasticVis allocator 正向 claim → 转"视觉 token 方差批内延迟外部性"信号验证（EV-VAR）；compatible co-batching 须在真实 GPU 胜 5 baseline 才作主线** | EV-1e（见上）终判 negative 已证 per-request 视觉预算在连续批处理下架构性失败；但负结果暴露的 batch-interference（高 k prefill 拖慢同批 decode）是可独立测试的机制。user 要求把它作为**可证伪预测信号**严格验证，而非直接当方法 claim。核心问题：方差/兼容性能否提供**超越 {Σk、SLO slack、token-budget batching}** 的预测力。 | **分阶段 gate**（plan ~/.claude/plans/warm-tinkering-bird.md）；**预注册 decision rules（跑前定稿，不接受跑后挪门槛）**：Stage1 GO2 = iso-ΣK 且 chunked-prefill ON 下 composition(var/compatibility) 使 per-decode-token 延迟变化 >10% & p<0.05 & 超噪声底；Stage2 GO3 = 嵌套回归 M1(var+compat) 显著优于 M0(Σk+slack+tokenbudget)（LR p<0.01）且蕴含 goodput 摆幅>run-to-run 噪声；Stage3 GO = compatible co-batching 胜**全部** {FIFO,deadline-only,token-budget,chunked-prefill,best-fixed}（mean±std ≥3 repeats）。**铁律**：① 旧 sim(independent-slot)按构造假设掉干扰→只作 null(residual=real−sim)，绝不作正向证据；② 既有 trace 无 batch 组成→必须新跑带 step 级 logging 的受控实验；③ chunked-prefill 必须是真实可测条件，不可假设掉；④ enforce_eager 全条件统一。env `qwen3vl_clean`(vllm0.19 V1-only)。Stage1+2 合计 <6 GPU·h 自主；Stage3 可能 >6 GPU·h → 找人。Stage0 进行中：serve_bench 加 `--log-step-composition`(monkey-patch vllm0.19 `Scheduler.schedule` 存 `SchedulerOutput.num_scheduled_tokens`/`scheduled_new_reqs`) + `--chunked-prefill{on,off}` + force `enforce_eager=True` | —

## 2026-07-13 | EV-VAR Stage 1 | **❌ NO-GO（high confidence）：视觉 token 方差"批内延迟外部性"信号被证伪；H_var rejected at foundation；不进 Stage 2/3** | Stage 0 primitive 验证通过（serve_bench `--log-step-composition` commit 14c3645，monkey-patch vllm0.19 AsyncScheduler.schedule 存 SchedulerOutput，3 图 smoke step0 正确读出 k_i{576,144,576}/phase/sum_k/var_k；--chunked-prefill off 不崩）。Stage 1 open-loop step-log 回归：4 cell = {HOMO k=360 / BIMODAL k=144,576} × {chunked ON/OFF}，**同 seed 同到达 trace**（R=8 req/s 校准，22.3% mixed step），n=300 TextVQA mixed-SLO，max-num-seqs 32，enforce_eager 全统一，共 2048 steps。**M0(sum_k+n_members+chunked) vs M1(+var_k+max_k+n_prefill+n_decode)**：联合 LR F=32.9 p=9.2e-21 **但分解显示全部来自相位混合**——`{var_k,max_k}` 单独 F=**0.179 p=0.836**（**k 组成零预测力**），`{n_prefill,n_decode}` 单独 F=98.6 p=1e-22（prefill 成员比 decode 贵=工作体积，token-budget 已捕获）。max_k 系数负（bimodal 更快，反方向）。iso 控制 sum_k+n_members 后 BIMODAL decode step 不比 HOMO 慢（chunkedON −0.27ms / chunkedOFF −1.27ms，反方向）。**chunked-ON 残余 k 组成 F=0.202 p=0.817**（标准缓解后无残余）；高并发 n_members≥12 F=0.85 p=0.43。signature(M0 残差) hi/lo=0.942<1.10。VIF var_k=max_k=8.2（共线，如预警）。 | **结论：H_var 被证伪。EV-1e 的"批内延迟外部性"=纯 sum_k（总 token）效应，无方差/组成信号可供 scheduler 榨取；chunked-prefill 封顶 sum_k/step → 每 step wallclock 由 n_prefill/n_decode（工作结构）驱动，与 k 方差无关，token-budget batching 已控此。按预注册规则 + 用户指令"信号成立才继续"，停止 variance 主线，不进 Stage 2/3（省 GPU）。** 残留 caveat：纯 deadline-routing 重排序（不依赖方差信号，区别于 EV-1e 的 k-assignment）未被本 step-latency 测试覆盖；但 (a) EV-1e 的 k-assignment 版（即 deadline-aware）已 GPU 输，(b) chunked 下 step latency=budget 驱动，重排只能重分布不能减总量，(c) 与既有 SLOs-Serve/AdaServe(文本域 per-req budget-for-goodput) 重叠 → 判 likely non-viable。**待 user 定**：A 折入 v2 论文 negative-finding 节｜B 独立 negative-result 贡献｜C 关闭 variance 线，回 v2 测量论文为主线｜D 纯 deadline-routing 当**独立新假设**先 novelty 扫描再定。产物 runs/ev_var/（gitignored，含 stage1_results.json DECOMPOSITION 块）+ src/ev_var/{analyze_stage1,analyze_decompose,analyze_hiconc,spotcheck,stepmix_report}.py（已提交）。GPU 仅用 ~15min | —

## 2026-07-13 | 方向决策（user）| **v2 测量论文升为主线；method search 收束；EV-1e + EV-VAR Stage-1 两个 negative 折入 v2 negative-finding 节** | EV-VAR Stage 1 证伪 variance 信号（见上）+ EV-1e allocator 终判 negative → 两条 method 线（per-request allocator、variance/compatible-co-batching）在真实 GPU 均不成立，**共同根因 = total-token(sum_k) 主导**（连续批处理 + chunked-prefill 下，per-step 延迟由工作结构而非 k 方差驱动，无 serving-signal 可榨取）。user 在 A/B/C/D 中选 **C变体：v2 测量论文为主线**（非 D 继续 method search）。drafts/paper_v2.md（measurement-led，9表5图47ref，可独立投：0/37 served-throughput + 3 发现 + c64 goodput）目标 Q1/Q2；两条 negative 折成"方法空间探索与界定"节强化方法学严谨性（实证界定 serving-signal-based per-request/variance 方法在连续批处理下被 total-token bound，不可行）| 下一步：评估 v2 论文当前状态 → 规划 negative 节整合 + 投稿就绪度 → 执行。新主对话从 drafts/paper_v2.md + STATE 起 | —

## 2026-07-13 | v2 novelty claim 推翻 | **⚠️ ERA(arXiv:2606.31982, Jun 2026)在 vLLM 内测了 served throughput → "0/37 report served throughput inside a production serving engine" 头条 claim 被打破（字面变 ≥1/38）；触发 charter §6 claim-推翻升级找人** | 子 agent 读 ERA 全文含 Table XIII：ERA 是 VLM 视觉 token 压缩器（training-free，projector/LLM 边界 prune+merge + rectified attention），**在 vLLM 内** LLaVA-NeXT-7B batch{256,48,1} 报 amortized E2E time/req（23.9ms@B256→~42 req/s served）、prefill 4.3×、decode tok/s、KV/req 5×、peak mem 244→60GB —— 明确测了 production engine 内 served throughput，并自标"deployment-oriented improvements with vLLM"。ERA 不在 37 survey 内（survey 截止 2026-07-01/03，ERA 2606 存在但漏；CARES 2510.19496 也漏）。**头条 0/37 字面被推翻。** ERA 漏的：continuous batching + online arrivals（静态 batch）、TTFT、p99 tail、goodput@SLO —— salvage 只能 sharpen claim 到这些。RTP-LLM(L401 已修为 throughput-engine-no-pruner)、CARES(offline-only) 不威胁，ERA 才是真威胁。 | **待 user 定**：A 先**全量 novelty re-scan**（ERA 被漏=survey 有 gap，scope 是否还有 ERA-like 威胁）再 sharpen claim 到"0/N report goodput/p99/TTFT under continuous batching + online arrivals"(ERA 漏的) + 以 framework 广度(2引擎×2架构×4压缩器×goodput-Pareto×c64)主导｜B 立即 sharpen、re-scan 后置（风险：再遇威胁又改）｜C 暂停 v2 重评估(measurement 是否还够 Q1/Q2 vehicle，或转 negative-finding 主线)。**re-scan 现为必要非可选。** 已停 v2 自主改动，升级找人。| —

## 2026-07-14 | 方向决策（user）| **重锚创新型论文（新方法）；方向 = pre-merger pruning on Qwen3-VL native merger；v2 降 fallback；用 EV-VAR 纪律 gated 验证** | 用户 2026-07-13/14 明确要创新型（新方法）论文而非测量论文（charter 原目标即"新方法"；v2 测量是 fallback，且 ERA 打破其头条）。在前述 A/B/C 中用户选 reassess 并指向创新。Brainstorm 后定 **pre-merger pruning**：在 Qwen3-VL 原生 2×2 merger 之前剪视觉 token，使 pruner 与 merger 互补（merger 压缩 survivors 而非与 pruner 争同一批 post-merger token）。动机 = F4（post-hoc 在 Qwen3-VL 衰减 1.08×→1.29×，因 merger+pruner 替代）。**正交于全部失败尝试**：非 boundary TF selector（避开 OCR 三连败）非 scheduling（避开 allocator/variance 的 total-token bound）—— 是 compression-architecture 维度。 | Gate（cheap-first，三通过才写论文）：① novelty scan（pre-merger-on-native-merger 是否未被做，对比 VisionZip/FasterVLM/VTC-CLS/ERA 等的 prune stage & arch）② feasibility（Qwen3-VL merger 位置 + vLLM 0.19 能否 hook pre-merger 不 fork）③ GPU go/no-go（pre-merger vs 现有 post-merger proxy on served 吞吐+精度，Qwen3-VL）。失败则 ~1 GPU·h 止损转下一方向。两 agent（novelty / feasibility）进行中。 | —

## 2026-07-14 | pre-merger 验证进展 | **Novelty=GO（VisionZip 经 GitHub 源码逐行核实实为 POST-merger → pre-merger cell 空，差异化成立）；Feasibility=GO（hook 策略 + 复用 v2_p1 现成 post-merger hook）；但 throughput 动机被 F2 削弱 → go/no-go 转测 accuracy at deep compression** | novelty scan 初判 PARTIALLY（snippet 误把 VisionZip 标 pre-merger，似直接碰撞）。**子 agent 读 VisionZip 官方 GitHub `Qwen2_5_VL/qwen2_5vl_visionzip.py` 逐行核实**：VisionZip **保留** native merger、在**所有 patch** 上跑 merger、**POST-merger** 选 dominant token（vision self-attn、text-agnostic、无 served-throughput、无 substitute framing）→ **pre-merger × native-merger cell 确实空**；差异化成立 = stage(prune merger input vs output) + signal(query vs text-agnostic) + framing(substitute)。PixelPrune=pre-ViT pixel；ERA=post-merger LLM-side，均不占此 cell。Feasibility：forward-pre-hook on `visual.merger`+3 `deepstack_merger_list` 同 mask（hard part=deepstack 一致性）+ recompute mrope + patch `get_num_image_tokens` + enforce_eager；复用 v2_p1 现成 post-merger hook 作 baseline。**动机修正（重要）**：notes/v2_p1 §F2 已示 merger+ViT=仅 10% TTFT、post-merger 不可简该 10% → pre-merger 的 **throughput 优势小**（只省部分 10%）；F4 放大差(Qwen3-VL 1.29× vs LLaVA 2.22×)主因 baseline token 数非 stage。故 go/no-go 转测 **accuracy at iso-token esp 深压缩**（select-before-lossy-merger 保精度假设）。 | go/no-go 实现中（agent a0e649）：Qwen3-VL GQA+TextVQA n=200，pre vs post × keep{50,25,12.5}%→{128,64,32} tok，accuracy+req/s，iso-token 精度对比 esp deep(32)。GO 若 pre 在深度处 ≥2-3pp 胜 post 且 gap 随深度增。**诚实护栏**：若 marginal，不强行当强创新论文。 | —

## 2026-07-14 | V3 pre-merger go/no-go | **✅ GO（强结果，verified from artifacts）：Qwen3-VL pre-merger 在 text-dense 深压下 iso-token +44pp 胜 post-merger；GQA 上 post 胜；机制=lossy merger 破坏 text 高频；workload-conditional。创新论文 spine 成立** | go/no-go（n=200 Qwen3-VL-8B，iso-token pre(C) vs post(B)，L2-norm text-agnostic selector 控变量隔离 stage 效应，enforce_eager，max_num_seqs=16；hook=`visual.merger`+3 `deepstack_merger_list` 同一 cached mask over merge-units，mrope recompute，deepstack concat 无 mismatch，smoke PASS 200/200 无 NaN）：**TextVQA(text-dense) pre 大胜** keep{50,25,12.5}%→ptid{387,203,111} C{0.75,0.695,0.615} vs B{0.51,0.255,0.175}，iso-token Δ{+24,+44,+44}pp，深度处 C 保 0.615/0.82=75% baseline 而 B 仅 0.175/0.82=21%（**3.5×** retention）。**GQA(object-QA) post 胜**（Δ{−2.5,−6.0,−5.5}pp）。throughput pre≈post（如 F2 预测，merger 仅 10% TTFT），深点均 ~1.55× baseline。**机制**：native 2×2 merger 是 **lossy 聚合**，破坏 text 高频细节 → post-merger 在已退化特征上选 → 灾难掉点；pre-merger 在 raw patch 上选 → 保 text。object-QA 上 merger 的 learned 聚合反而 helpful → post 胜。= **未知的、机制解释的、workload-conditional 的 stage 效应**（比 uniform win 更强且更诚实），在已验证空的 pre-merger×native-merger cell。数字已从 v3_premerger_results.txt + cell JSON 亲核。 | **创新论文 spine**：pre-merger 保 text/OCR 深压精度（post 灾难失败）+ lossy-merger 机制 + workload-conditional → 暗示 **adaptive stage 方法**（text-dense 用 pre，object 用 post）。代码 `src/v3_premerger/`（runs/v3_premerger_* 为执行产物，gitignored）。**下一步（强结果化）**：① 更强 selector（L2 是 probe-grade；+44pp 稳健但可扩 margin、缩 GQA 损）② 跨架构（Qwen2.5-VL/InternVL3 同 merger 结构，效应应复现）③ 更多 text-dense（OCR-Bench/DocVQA/ChartQA，效应应更强）+ object suite（GQA/POPE/MMBench）刻 workload 条件 ④ n=500 mean±std ⑤ vs VisionZip(post-merger SOTA) baseline（必须 beat on text-dense）⑥ 写论文。GPU 仅 ~11min。| —

## 2026-07-14 | V3 suite characterization | **workload-conditional stage law 确认（按 text-density 单调）跨 5 benchmark；DocVQA(headline) 因 max_model_len+OOM 失败、修复重跑中** | 跑 pre(C) vs post(B) vs baseline(A) 跨 5 benchmark（n=200 Qwen3-VL-8B，iso-token，L2 text-agnostic selector 控变量，keep{25,12.5}%，max_num_seqs=16）。**@12.5% keep，C−B（+=pre 胜）按 text-density 单调递减**：TextVQA(场景文字)**+44.0** → MME(yes/no 感知)**+3.5** → MMBench(感知MC)−1.5 → ScienceQA(图+文MC)−2.0 → GQA(物体/空间)**−5.5**。**机制一致性=单调**（gap 随 text-density 降而降）= 与"lossy merger 破坏高频 text 细节"机制预测吻合。pre 胜 text-dense（TextVQA/MME），post 胜 object/MC（GQA/ScienceQA/MMBench）。**= 论文科学贡献**：一个 stage×workload law（非 uniform win，更诚实更强）。runner 已扩 mme/mmbench/scienceqa/docvqa 评分（`acc` key，非 `accuracy`）；v3_suite_analyze.py 出 map（runs/v3_suite_map.json）。**DocVQA（文档OCR，最 text-dense，headline）失败**：A 因 max_model_len=8192<16263 token 拒；B/C 因 max_num_seqs=16 并发大文档 prefill OOM。修复中（max_model_len→32768、max_num_seqs→4）重跑——预期 pre 胜 >>+44pp。 | 下一步：DocVQA 结果 → 跨架构(Qwen2.5-VL/InternVL3) → n=500 mean±std → vs VisionZip(post-merger SOTA) → adaptive-stage 方法（text-dense 用 pre、object 用 post）→ 写论文。| —

## 2026-07-14 | V3 DocVQA 结果 | **DocVQA（文档OCR，最 text-dense）确认 pre 大胜：@25% keep iso-token pre 0.725 vs post 0.39 = +33.5pp（CLEAN）；@12.5% post cell 在 16k-token 巨图上 vLLM 崩溃(ptid=0)→ 报的 +61pp 不干净，不作 claim。机制强证：post 从 baseline 0.77 崩到 0.39，pre 仅降到 0.725** | DocVQA baseline acc=0.77（mean_ptid=4154，巨文档；修 max_model_len=32768+max_num_seqs=4 后可跑）。**@25% keep（iso-token ptid=1054）B post=0.39 / C pre=0.725 = +33.5pp CLEAN**——post 从 0.77 崩到 0.39（lossy merger 破坏文档高频文字），pre 仅降到 0.725（raw patch 保文字）。@12.5% keep：C pre=0.61（ptid=538）；**B post 崩溃**（n_ans=0,ptid=0；diag 示 pruning 算了合理 survivor 数 166-497，但 forward 在 16k-token 巨图 @87.5% prune 下 vLLM internal error）→ **+61pp 非干净对比（pre@538 vs crashed post），不 claim**。**6-benchmark suite @25% C−B**：DocVQA +33.5 / TextVQA +44 / MME −0.5 / MMBench −2.5 / ScienceQA −0.5 / GQA −6.0（pre 胜 text-dense DocVQA/TextVQA；post 胜 object/MC）。@12.5% suite：DocVQA(崩) / TextVQA +44 / MME +3.5 / MMBench −1.5 / ScienceQA −2 / GQA −5.5。**诚实注意**：@25% DocVQA(+33.5)<TextVQA(+44) 略非单调（L2 selector 在 DocVQA 4k-token 上行为差异？n=200 噪声？需更好 selector/更大 n）；大趋势（text-dense→pre 胜，object→post 胜）清晰。runner 已加 `--max-model-len`，稳定；代码 commit。 | 下一步：修 @12.5% DocVQA post 崩溃（bound image size 或 post-hook on 巨图）拿干净深度对比 → 更强 selector（L2 是 probe-grade）→ 跨架构(Qwen2.5-VL/InternVL3) → n=500 mean±std → vs VisionZip → adaptive-stage → 写论文。| —

## 2026-07-14 | V3 tighten 结果 | **部分成功 + 暴露 2 隐患：@25% core solid（TextVQA n=500 +46.6pp clean、selector 鲁棒、GQA 对比），但 DocVQA n=500-with-cap post 崩溃（作废，用原 n=200 +33.5pp）、深@12.5% post 仍崩、seed 重复 std=0(greedy 无方差)** | tighten 跑（n=500 L2 key benches + 3-seed + attn 鲁棒，runner 加 `--selector{l2,attn}`/`--max-pixels`/`--seed`，attn=centroid-distance 代理因 vLLM0.19 flash-attn eager 下真注意力不可得）。**CLEAN（n=500）**：TextVQA@25% pre 0.738 vs post 0.272 = **+46.6pp**（iso-token 213，stderr±0.02，最硬 headline）；GQA@25%/@12.5% post 胜（−0.008/−0.058）；selector 鲁棒：l2 vs attn 在 TextVQA/GQA stage-effect 符号一致（pre 胜 text、post 胜 object 不随 selector 变）。**⚠️隐患①**：DocVQA 全部 post cells 又崩（acc=0,ptid=0）——`--max-pixels` cap 反把 @25% 也搞崩（原 n=200 无 cap 时 @25% post=0.39 正常）→ tighten 的 DocVQA 作废，**干净 DocVQA headline 仍用原始 n=200 @25% pre 0.725 vs post 0.39 = +33.5pp**；深@12.5% post 在大图上崩（cap 与否都崩）= post-hook 大/激进 cell 不稳定，未修。**⚠️隐患②**：3 seed acc 完全相同（std=0）= greedy(temp=0)确定性，seed 重复无方差信息；真误差棒应用 n=500 binomial stderr(±0.02)，非 seed 方差。 | **净**：@25% core solid（TextVQA +46.6 n=500 / DocVQA +33.5 n=200 / selector 鲁棒 / GQA 对比）。写论文前必修：①debug post-hook 在大图深压崩溃（拿干净 @12.5% 对比）②temp>0 或 bootstrap 拿真误差棒。代码已 commit（runner+ tighten.sh+ tighten_analyze.py）。 | —

## 2026-07-16 | V3 Task1 | **DocVQA post crash 根因精化：encoder_cache_size = max_num_batched_tokens（非 mm_processor_cache_gb）；runner 加 --max-num-batched-tokens 修复，smoke n=1 验证通过，n=200 重跑中** | 原猜"vLLM encoder cache budget 8192 太小"方向对但参数名错。vLLM0.19 V1 源码链路：`config/scheduler.py:235` `self.encoder_cache_size = self.max_num_batched_tokens`（不可单独配，绑死）；`v1/core/encoder_cache_manager.py:313` `encoder_cache_size = max(scheduler.encoder_cache_size, max_tokens_per_mm_item)`；`v1/engine/input_processor.py:409` `if embed_length > self.mm_encoder_cache_size` → 抛 `ValueError: ... exceeds pre-allocated encoder cache size {8192}`。`mm_processor_cache_gb`(默认4GB,GB单位)是 processor **结果**缓存，与 token 级 encoder cache 无关——STATE/Task1 旧提的 `--mm-processor-cache-gb` 是错路。runner 原本只传 max_num_seqs，未传 max_num_batched_tokens → vLLM 默认 8192。**双重故障**：A baseline 触发 ValueError 但 per-sample fallback 救回 193/200(skip7,acc0.7617)；B post r0.875 额外开销 → 峰值 OOM(GPU 43/44.45G满,1次)→ 引擎卡死、encoder_cache_usage 卡 0.9428 → per-sample 连坐 skip 200/200。**修复**=runner 加 `--max-num-batched-tokens` flag(default None→vLLM默认,DocVQA用32768)+ 接入 LLM 构造 + 写入 result JSON。✅smoke n=1 post r0.875 + mnbt32768 通过(n_ans=1,ptid=184,diag.fires=1,无 ValueError/OOM)。n=200 batched 重跑中(后台)——若仍 OOM 则降 max_num_seqs(4→2)换显存余量。claim scope 须标「同 max_num_batched_tokens 配置下」，crash 是环境配置非方法缺陷。 | 代码已改 runner；待 n=200 验证后出 clean A/B/C @25%@12.5% 对比表，commit。 | —

## 2026-07-16 | V3 Task1 | **B post DocVQA r0.875 n=200 CLEAN 验证通过：@12.5% keep pre 0.61 vs post 0.135 = +47.5pp（首次干净深度对比）** | runner --max-num-batched-tokens 32768 修复生效，batched n=200 全程无 OOM/ValueError（0 error, 4 reqs concurrent, KV 1.5%, GPU 39.4/46G 健康 6.6G 余量 vs crash 时 43/44.45G 满）。结果：B post r0.875 acc=0.135, n_ans=200, skip=0, ptid=538, wall=244s（vs crash: acc=0, skip=200, ptid=0, wall=45s 全静默skip）。**对比表**：baseline A=0.7617(待 iso-config 重跑) / B post @0.875=0.135 / C pre @0.875=0.61 → **+47.5pp CLEAN @12.5% keep**，比 @25%(+33.5pp) 更深——完全吻合"text-dense 越深压 pre 越胜"机制（post 在 12.5% survivor 下 lossy merger 严重失真+深压双重毁 text→0.135，pre raw patch 保文字→0.61）。机制强证。 | 待 B r0.75 重跑 + A baseline iso-config 重跑后出完整 clean 表（@25%@12.5% A/B/C）+ commit。 | —

## 2026-07-21 | V3 Task4 adaptive router 设计 + 4a 探针 | **Task 4 分三阶段：4a 廉价信号探针（oracle per-sample 上限 vs 廉价信号 gap，几乎零 GPU）→ 4b `--mode auto` 同进程 per-sample 路由 → 4c 误差棒/信号消融。4a 探针代码就绪；现有 cell 无 per-sample 数据 → 需 GPU 补测 4 cells（<5min）；GPU 被 sues YOLO 占 → 挂自动等待，空闲即补测+分析** | 主线 V3 已证实 workload-conditional stage 效应（text-dense→pre 大胜，object→post 胜，跨 6 benchmark 单调）。Task 4 = 把该效应变成自适应方法。核心科学问题先问清：**廉价输入信号能否 per-sample 预测该用 pre 还是 post？** 能→adaptive router 成立；不能→framing 改（workload-conditional law 本身作主贡献，或需更重学习型 router）。设计纪律 = cheap-first：4a 先算 oracle 上限 + sweep 信号，逼近 oracle 才建 4b 重工程，避免盲投 GPU。**4a 探针结果**：现有所有 cell（v3_premerger_cells/v3_tighten_cells）仅存聚合 acc、无 per_sample 字段 → oracle=mean(max(pre_i,post_i)) 不可从聚合反推。已 patch runner 输出 per_sample(id/correct/question/gt/answer/prompt_token_ids)；router_probe.py（CPU 分析：always-pre/post + oracle 上限 + sweep 三类信号 a=29词 OCR关键词正则→pre、b=ptid阈值、c=a OR b；自动选 pooled 最优，判据 gap→oracle <2%进4b/<5%可进/≥5%需更重信号）+ router_probe.sh（TextVQA/GQA × pre/post n=200 串行，同 subset 同 seed 可按 id 对齐）就绪。commit b7f57c4（用户 Code-Yan-ZX 名义，已核无 AI 署名）。**4b 风险已标（子 agent 未点破）**：runner mode 是进程级一次性锁死、无 per-request 分支；且 vLLM continuous batching 下同 forward 可混不同 mode 请求 → thread-local 路由会乱 → 4b 须 `--max-num-seqs 1` 先保正确性，再谈 batching 下 per-image 路由（pre 的 mask 本就在 merger 输入上 per-image 算，或有救）。**自主执行说明**：补测 <1 GPU·h，在 charter 自主范围内（未触凭据/>6GPU·h/claim推翻/投稿），故挂监控自动跑、不升级。| 下一步：GPU 空闲→router_probe.sh+.py→读 gap 决策：逼近 oracle 则设计 4b（seq=1 正确性→batching）；不行则重估 Task 4 framing。| —

## 2026-07-21 | V3 Task4 4a 结果 + 贡献重框 | **4a 探针成功（GPU 空闲自动跑通）：pooled(TextVQA+GQA n=200×2) always-pre 0.5075 / always-post 0.3175 / oracle 0.59 / 最佳廉价 router(ptid≥94) 0.54（胜两固定策略、距 oracle 5pp）。决策=贡献重框为「workload-conditional stage law（发现）+ 廉价 text-density adaptive stage router（方法）」；不追 5pp oracle gap；4b 改离线扩证（全梯度+workload/sample 分解）而非急建 vLLM per-sample `--mode auto`** | 4a 解读：①每 benchmark 内一个 stage 几乎完全主导（TextVQA pre 0.695≫post 0.255，oracle 仅 0.74；GQA post 0.38>pre 0.32，oracle 0.44）→ 路由价值主要在 **workload 级**，per-sample 残差小（+4.5/+6pp）。②廉价 ptid 到 0.54 主因分隔了 workload（两 bench 图 ptid 分布不同），非细粒度 per-sample。③router 真卖点 ≠ "+3.25pp over always-pre"，而是**对未知/混合流量的鲁棒性**：固定 post 在 text-dense 灾难崩（0.3175），router 避免掉悬崖（0.54）。**诚实护栏**：5pp 恰在预注册判据边界（<2%进/<5%可进/≥5%需重信号）→ 取"可进"侧，因核心 claim（router 胜两固定）成立 + 发现本身强。**为何不急建 `--mode auto`**：精度 claim 离线路由分析即可成立（标准做法）；throughput claim 弱（ERA 打破 + merger 仅 10% TTFT，v2 已降级）→ 啃 vLLM continuous-batching per-sample 路由是 over-engineering。改先离线扩证（全梯度+n+workload/sample 分解+误差棒），便宜（GPU 现空闲 <1 GPU·h）高价值。**自主执行**：<1 GPU·h 在 charter 范围内，未触升级条件。| 下一步：子 agent 验证 4a id-对齐 + 扩 {textvqa,docvqa,gqa,mme}×{pre,post} per-sample + 分解 → 据分解定论文 spine 与是否需 seq=1 demo。| —

## 2026-07-21 | V3 Task4 4b 全梯度结果 + spine 待 user 定 | **4b 完成（子 agent 工具链 API 故障→主窗口直跑；已核 id 对齐+oracle 复现，docvqa post 本次 r0.75 未崩）。4 benchmark 全梯度干净。pooled(N=774)：always-pre 0.6344 / always-post 0.4522 / oracle 0.7016 / 廉价 router(ptid≥94) 0.6550（胜两固定：+2.1pp over pre、+20pp over post；距 oracle 4.65pp）。分解：workload 级仅占 oracle 增益 26.9%、sample 级残差 73.1%（query 依赖、廉价信号够不到）。OCR 关键词路由更差(0.539<always-pre)，有效信号=ptid。spine 待 user 定** | per-bench：textvqa pre0.695/post0.255/oracle0.740；docvqa pre0.725/post0.390/oracle0.795；mme(n=174,26 skip) pre0.822/post0.833≈tie/oracle0.851(router0.845 超两固定)；gqa pre0.320/post0.380/oracle0.440。**反直觉关键**：廉价 router≈workload 级路由(0.655≈0.6525)；oracle 的 73% 增益是 per-sample、query 依赖（哪段文字/区域对该题关键），ptid 等图像级廉价信号够不到，需 query-aware 重 router。**重框洞察**：真正强的贡献不是"+2pp router"，而是 ①lossy-merger 机制 ②workload-conditional stage law（按 text-density 单调）③**post-merger SOTA（VisionZip 类）在 text-dense 深压灾难崩溃（docvqa 0.77→0.39、textvqa→0.255）、pre-merger 是鲁棒修复**=field-relevant 强发现。always-pre 已是鲁棒安全默认(0.6344,从不崩)。**spine 候选**：A(推荐)发现主导+pre-merger 为方法+adaptive stage 作实用节；B 坚持 router 主线、投 query-aware 重 router 够那 73%；C 两者并重（篇幅/工作量更大）。**无论哪个都需补**：vs 真 VisionZip(post-merger SOTA)对比 + n=500 误差棒(binomial stderr，greedy seed 无方差)。**自主说明**：4b GPU<1h 在范围内；子 agent 连续 3 次 API 故障(InvalidParameter Model not exist)→主窗口直跑属工具不可用之妥协。**spine=论文核心 claim，升级 user 定（合 DECISIONS 既有"待 user 定"惯例）**。代码 src/v3_premerger/router_probe_full.py(commit 待核)。| 待 user 定 spine→据此推进 VisionZip 对比+n=500。| —

## 2026-07-21 | V3 spine 决策（user）| **user 选 A=发现主导 + pre-merger 为方法**：headline=lossy-merger 机制 + workload-conditional stage law + post-merger SOTA(VisionZip 类)在 text-dense 深压灾难崩溃、pre-merger 鲁棒修复（field-relevant 强发现）；adaptive stage selection（ptid 廉价、workload 级）作实用节，router +2pp 如实报不夸大。放弃 B(投 query-aware 重 router 够 73% per-sample)、C(两者并重)。**理由**：4b 示廉价 router 仅 +2pp over always-pre、oracle 73% 增益 query 依赖难预测→router 当 headline 弱；stage law + post-merger 脆弱性是强发现，且 pre-merger×native-merger cell 已核实空（novelty 成立）。**自主推进下一步**：① VisionZip-style 补 DocVQA（headline 完整覆盖两 text-dense bench）② docvqa n=500 pre/post + binomial 误差棒（textvqa n=500 已有 pre0.738/post0.272）③ stage-law 图表整合 ④ 写论文。均 <6 GPU·h 在自主范围。| 推进中。| —

## 2026-07-22 | SOTA 决胜小矩阵（本轮）| **ChartQA/OCRBench n=200 建成并全矩阵跑完（Qwen3-VL-8B, pre/post/VZ-style @25/12.5%, 21 cell 零失败）+ VisionZip 官方 audit + 三方法 audit，产物 `drafts/v3_sota_matrix.md`（final）** | ① **OCRBench = text-dense 第三确证**：@25% pre 0.580 vs post 0.165（+41.5pp, 9.5σ）、@12.5% 0.380 vs 0.075（+30.5pp, 7.8σ）；子技能分解显示 stage-vs-budget 分裂下沉到类型级（纯文字识别 pre 保 baseline/post 清零 1.0 vs 0.0；手写公式同分 = budget regime）。② **ChartQA = 新第三 regime：budget-dominated**（非 bug，per-sample 核实：压缩下模型转 hedging 长答 1.1→7.7 词 + 读邻近错值）：@50% pre0.39/post0.335、@25% 三方同分 0.190（pre/post 各对 38 题仅 20 重叠=同分巧合）、@12.5% 0.15/0.095（+5.5, 1.7σ 不显著）→ 论文报"stage law 管何处 pre 胜、budget 管是否可行"，不 overclaim 全单调。③ **VisionZip-style ≡ post dom-only：11/11 cell 完全一致**（含本轮补的 post-mode textvqa/docvqa @25/@12.5）→ context tokens（连续组均值）零增益、崩溃由 stage 单独解释；官方 dom-ratio/ctx 实现差异实证无关。④ **VisionZip 官方判定 (c) 不可跑**（`JIA-Lab-research/VisionZip` CVPR'25：仅 CLIP-LLaVA+Qwen2.5-VL、无 vLLM、Qwen 变体需 attn 物化 OOM 风险）；代码级再确认 post-merger；**作者自家 README Qwen2.5 OCRBench@50% 81.5→70.5（−13%）= 官方数字里的 text-dense 崩溃 onset**（25% 未公布）→ 论文 SOTA 列 = 我方 same-model port + 官方 mismatched reference。详 `drafts/visionzip_gap_report.md`。⑤ **QuietPrune/Hi-Lo Prune/IF-Prune 均不可作同 budget 公平 baseline**（无码/空仓/需训 20–60 GPU·h）→ 不跑 IF-Prune InternVL recipe（模型失配非公平，决策：弃）；**Hi-Lo Prune 挂 code-release watch**（论文支持 Qwen3-VL，最佳未来 baseline）。详 `drafts/baseline_methods_audit.md`。⑥ scorer：chartqa relaxed ±5% / ocrbench normalized containment，lmms-eval 逐式移植、gt 自测 200/200（commit f23841a）。⑦ 定性 10 例 `drafts/qualitative_examples.md`（"$1.3 BILLION→million" 单位错 post+VZ 同犯 pre 对）。caveat 红线见 matrix §6（ChartQA/GQA gap 不显著只报方向；scope=Qwen3-VL 单架构）。| 全部 commit+push（用户名义）；下一步=可起草论文（results 骨架齐）或补机制图（retention-vs-compression 曲线）。| —

## 2026-07-23 | 补强半天（user 选"先补强再起草"）| **三项零/低 GPU 补强完成，故事从"完整"升级"扎实"，进入起草** | ① **跨 selector 不变性**（attn=global-centroid 另一打分族, n=200 @25%）：OCRBench pre.480/post.170(+31)、DocVQA .68/.365(+31.5, attn-post 旧崩溃 BIG config 修复)、ChartQA .19/.165(≈0)、+已有 TextVQA/GQA attn → **stage law 与 budget regime 均 selector-invariant**（非 L2 artifact），写作升级为 robustness 节（`runs/v3_attn_robust/`）。② **机制可视化双层发现**（`scripts/mechanism_token_survival.py`，几何核实 patch16→unit32px、PRE 打 deepstack_0 输入特征）：DocVQA **支持**（PRE 保留笔画 Sobel .931 vs 丢 .081；POST 避字 .148 vs 丢 .342；Jaccard .079）；TextVQA **location-level 不支持**（两方边密度相当 .31/.31）→ post 损伤在**特征值退化**非选位 → 机制升级为**双层**（文档=选位错导 / 场景文字=特征退化），TextVQA 反例如实写（不藏不利图）。③ retention 曲线图（`drafts/figures/retention_curves.{png,pdf}`）+ 效率 §7（stage 吞吐中性、压缩提速 +28~45%）。④ 剩余缺口（无法本轮消除）：单架构（Qwen3-VL scope 限定）、SOTA 对比 principle-port（已 audit 无公平第三方）。| commit+push 后 **起草 paper_v3**（nature-writing，证据→claim 映射，caveat 红线内）。| —

## 2026-07-23 | V3 M3 机制因果验证（自主 GPU ~25min）| **新增 runner `--mask-ranking swap`（post 前向 + pre 排序选择）+ M1/M2 规模化（n=64/bench 确定性样本）；因果控制 swap≡pre 成立，机制从「诊断」升级为「因果证明」** | M3 控制：因 unit 等价（被保留 unit 的 merged token 与 stage 无关），post 前向+pre 排序**必等于** pre 标准 → 证明 pre>post 差距 100% 是排序效应而非前向信息破坏。实现 setup_post_merger_swap（hook deepstack_0 只读捕获 pre 分数 + 用 pre 排序过滤 merged tokens）+ PreMergerPruner(mask_ranking="swap") 用全 merger 跑全输入得 post 排序；dry-check 双族全过。结果（官方离线 rescore）：**DocVQA swap ANLS 0.4650 = pre 0.4650 完全相同、200/200 答字节一致、ptid 200/200 一致**；**TextVQA swap VQA-acc 0.6033 vs pre 0.5983（Δ+0.005, paired SE 0.005）、198/200 答一致**，2 差为确定性 ε 数值（merger GEMM batch-size 不同，swap 两次重跑 200/200 完全一致故非调度噪声）。vs post 仅 40/63 重合。diag fallback=0/leftover=0。M1：merger 几乎重排（ρ docvqa 0.14 / textvqa 0.33 / gqa 0.36；Jaccard@25% 0.18/0.24/0.28，text-dense 最差）。M2：text-dense 反文本方向成立（DocVQA ρ(shift,edge)=0.44、组(a)pre-only 边缘 0.64 vs(b)post-only 0.12、92% vs 35% 超中位；GQA ρ=0.036≈0）。**诚实 nuance 保留**：docvqa=selection-misguided 完整支持；textvqa=排序乱(M1)且为 gap 全部来源(M3)但 M2 文本方向中等+feature-degradation budget 层(0.60 vs 0.86)；gqa 近零。机制节写作升级为「M1 重排 + M2 文本方向 + M3 因果交换」三层，docvqa-yes/textvqa-partial 如实写。capture 10 张首轮 vLLM 请求级抖动失败→resume+retry 补满 64/64；consumed 计数缺口=V1 runner 级 embedding cache replay（绕过 hook 回放已 pre 选的剪枝 embed，故无害且反证正确性，仪表化 mini-run 证平衡）。| **自主执行**：GPU ~25min（capture 全 bench 单 load ~8min + swap 2 cell ~5min + 诊断/补满 ~10min）<6 GPU·h 在 charter 范围；未触升级条件（非凭据/非 claim 推翻——反而加固 claim）。产物 `drafts/mechanism_verification_report.md` + fig + stats + commit。| 下一步=据 M1/M2/M3 起草论文机制节。| —

## 2026-07-23 | 方法 gate：merger-aware hybrid 选择 + disagreement router（自主 GPU ~19min）| **gate=FAIL（如实）：hybrid(t=0.5)@n100 官方指标 textvqa VQA-acc 0.560≥pre 0.537 ✓（噪声内+2.3pp）、ocrbench 0.510<pre 0.590 ✗（−8pp≈2σ 真回归）、gqa 0.500 且 pre==post==0.510（短答协议下旧 containment"post 胜 pre 6pp"消失，无 gap 可闭）✗。无单一 text-frac 过 gate（t=1.0 ocrbench 追平 pre 但 textvqa 0.533≤0.537）。disagreement router 混合池(textvqa+docvqa+gqa n=192) 0.484 < always-pre 0.494 = ptid-router，距 oracle 0.576 差 8.2pp——dis 不分区制（gqa 中位 dis 0.63≈textvqa 0.64、docvqa 0.90 全局最高），pre 在图像级近乎占优（各 bench 84–97% 图 pre≥post），oracle 余量为 query 依赖。attn selector 不变性 ✓（pre 0.553>post 0.200, +35pp, 官方 VQA-acc）** | 建设：runner `--mode hybrid`（post 前向 + 协议集 A=top-k(PRE)∩top-k(POST) 全留 + 争议预算按 `--hybrid-text-frac` 路由：t 份给高 Sobel-edge 单元的 pre 排序、其余给低 edge 的 post 排序，恰保 k 个 iso-token；edge 从 encoder 自家输入像素重建 32px 单元 Sobel，免疫 encoder-cache replay 错配）+ `--save-unit-scores`（per-image Jaccard@k/Spearman/mean-edge 入 per_sample，离线 grid 计数 FIFO 防 replay 错位，92/100 附着 0 错配）；GQA subset 短答修复（_backup 留底）；16 cell 零失败（12 gate + 4 frac 敏感性）。**决策=不 scale hybrid、论文 spine 不变（user A：机制+stage law+post-SOTA 脆弱性/pre 鲁棒修复），gate 负结果如实写入论文方法节**（"试过用 merger-aware mask 与 disagreement router 击败 pre，pre 是不动点"——反而加固机制：排序知情的 post 级选择也无法改进承诺 pre 排序）。router 作负结果/机制旁证节。**升级 user 定**：n=200 确认 gqa pre==post 平局与否（n=100 SE±0.05）。产物 `drafts/method_gate_report.md` + `runs/v3_merger_aware/{hybrid_gate,router}/` + 脚本。| 下一步（main window）：据 gate 定论文方法节写法 + 是否 n=200 确认 gqa。| —

## 2026-07-23 | 评测修正（user 指令：TextVQA→官方 VQA-acc、DocVQA→官方 ANLS，离线优先；本条时序在 M3/方法 gate 之前）| **旧指标=ad-hoc containment + verbose 生成高估 ~100× → 换官方指标 + short-answer prompt 后主结论 HOLD 无翻转；GQA pre==post 假象纠正** | 离线重评分（`src/v3_premerger/official_scorers.py` VQA-acc/ANLS，self-test 过）发现：router_probe 等 cell 生成是 verbose 句子（median 132 字符），官方 VQA-acc/ANLS 对每个 pre 与 post 样本都≈0（containment 把"gt 出现在句中任意处"记对，高估 ~100×）→ **根因=runner prompt 只给 raw question、无短答指令**（ChartQA/OCRBench 因子集烤入"single word"指令故正常）。修=同惯例把 "Answer the question using a single word or phrase." 烤入 textvqa/docvqa（后补 gqa）子集（`scripts/fix_shortanswer_subsets.py`/`fix_shortanswer_gqa_subset.py`，_backup 留底）+ 重跑 baseline/pre/post @keep25% n200（`runs/v3_merger_aware/rescore_rerun/`）。**官方指标结果（HOLD）**：TextVQA VQA-acc baseline 85.8/post 21.5/pre 59.8 → **pre−post +38.3pp±4.5（~6σ）**（ret pre 70%/post 25%）；DocVQA ANLS baseline 97.6/post 20.0/pre 46.5 → **+26.5pp±4.5（~6σ）**（ret 48%/21%）。**⚠️claim 纠正：GQA 同病（verbose ~139 字符）→ 短答+exact-match 下 pre==post==0.51**，旧 containment"post 胜 object −6pp"是假象 → stage law 改"pre 弱占优、无 crossover"（随后被方法 gate n=100 复证）。**自主执行说明**：离线重评分零 GPU；重跑 ~6 cell <1 GPU·h 在 charter 范围；user 预授权"缺失数据才重跑"（verbose 预测对官方指标不可用≈缺失）；按 user"主结论反转立即汇报"已即时上报（诊断=生成协议假象、非方法翻转，故继续）。commit a3e7780(rescorer)+638c09a(子集修+重跑)。产物 `drafts/rescore_rerun_report.md`+`runs/rescore_official/`。| —

## 2026-07-23 | goal 接纳 + 全局排序（user /goal：RBM → CCF-B 主目标、CCF-A 扩展，submission-ready 方法论文）| **接纳并按 `runs/QUEUE.md` 串行推进：J0 数据+官方 scorer 准备（CPU 并行）→ J1 修通 Qwen2.5-VL-7B（mrope 根因已定位）→ J2 跨代 subset 矩阵+官方 rescore+GQA n=200 tie 双模型确证 → J3 机制跨代复制（swap/Spearman）→ J4 baseline port 探针（FastV/PyramidDrop，VisionZip≡post 已证）→ J5 query-aware 单次止损 gate → J6 效率表 → J7 官方完整 split 主表（headline cells 先行）→ J8 消融闭环 → J9 paper_v4 改写+venue 定** | 完成条件=user 五条：Qwen3+Qwen2.5 结论一致/完整官方 benchmark/强 baseline 充分/机制+效率消融闭环/submission-ready。**红线**：不跨模型宣称 SOTA；仅同模型同 harness 领先才写"超过现有方法"；VisionZip 官方数只作 mismatched 锚（延续 07-22 audit）。**测绘五 digest 结论**：①Qwen2.5-VL 崩溃根因=runner `_patched_pii` 整体替换 `_process_image_input` 绕过 vllm `recompute_mrope_positions`（Qwen2.5 block mrope [16,24,24] θ1e6 剪后三轴错位；Qwen3 interleaved 自洽），修法=剪后触发重算或 wrap 而非替换（qwen2_5_vl.py:1342-1397 现成路径）；runner `--model-family qwen2vl` 已全链路、baseline cell 正常(.886/.514)证仅压缩路径坏 ②VisionZip-style 11/11 cell≡post，官方码不可跑（无 vLLM/attn OOM）→ principle-port + 官方 mismatched 锚 ③官方 scorer 仅 TextVQA/DocVQA，补 OCRBench 五类/1000 + GQA 官方 normalize exact-match ④完整 split：GQA testdev 在盘，TextVQA/DocVQA/OCRBench 需下载 ⑤baseline 最小充分集=FastV（必选标杆）+VisionZip(port)+PyramidDrop（层内渐进），FasterVLM 可选；QuietPrune/Hi-Lo/IF-Prune 弃（延续 audit）。**fairness 定义**：同 keep ratio（相对每图自身 full token）+报平均绝对 token 数+统一 min/max pixels（patch 14 vs 16 需按 family 校准 iso-token）+PyramidDrop 折算等效均值；他文数字只作趋势锚注 mismatch。**数据纪律**：完整 split 全落 runs/data/（/media/disk2 1.4T 空；~ 已 90% 满勿写）。**预注册 QA gate（跑前定稿，不挪门槛）**：方法=question-conditioned pre-merger saliency——unit 过 merger MLP 入 LLM 空间 vs 问题词嵌入 cosine 得 qsim，s=(1−λ)·l2+λ·qsim，λ∈{0.3,0.5,0.7} 于不相交 64 条 dev slice（TextVQA+DocVQA）选；判据=Qwen3-VL 4 subset n=200 r=0.75 官方指标 GO 需 (i)4 基准均值 ≥ plain RBM +1.0pp (ii)≥3/4 基准各 ≥ RBM−0.5pp (iii)pooled 增益 z≥1.5（binomial SE）；NO-GO=冻结 plain RBM 为方法、QA 作 bounded negative 如实写、**不再搜索任何 hybrid/router 变体**（延伸 07-23 FAIL gate）；GPU 止损 ≤2 GPU·h。**升级点**：凭据/单 run>6GPU·h 训练/claim 推翻/投稿前；J7 估算 ~50-60 GPU·h 系逐 cell <6h 推理评测（非训练）不触升级，QUEUE 记估时+断点续跑。**统计**：full split 用 binomial stderr，z≥1.96 才 claim 领先、1.5–1.96 只报方向。| 任务表 #1–8 建；J0×2+J1 三子 agent 并行启动；每 checkpoint commit+push（Code-Yan-ZX，禁 AI 署名）。| —

## 2026-07-24 | J1 PASS：Qwen2.5-VL-7B 修通 | **mrope 修复验证通过，跨代验证解锁** | 根因=vLLM get_mrope_input_positions 按完整网格推进位置游标 vs 占位符已缩到 k→尾部文本继承 2D 网格位；block-mrope[16,24,24] 聚集错误崩溃、interleaved 容忍（跨架构差异核心实锤）。修=setup_qwen2vl_mrope_fix 按实际 k 推进游标（r=0 逐位退化原版、qwen3vl 不碰）。预注册 smoke 全过：GQA n50 **pre 0.680 / post 0.640**（containment，均≥0.40 ✓）、TextVQA pre 0.780、qwen3vl 回归 0.750 不变、mrope_fix diag 51/51 触发。**早期跨代信号=Qwen2.5 GQA pre≥post 弱占优（与 Qwen3 同模式）**。| GPU ~15min 在 charter 范围；claim 未推翻（反而加固机制）；J2 矩阵即发（21 cell + GQA tie n200 双模型 + 官方 rescore），J0a/J0b/J4 已交付（完整 split/官方 scorer/HF baseline harness 均 commit）。| digest `experiments/j1_qwen2vl_mrope_fix.md`。| —

## 2026-07-24 | J2：跨代一致性成立（官方指标 n=200）| **Qwen2.5-VL-7B 全矩阵 24 cell 零失败 + 官方 rescore：text-dense 三基准 pre 大胜（TextVQA +32.0pp / DocVQA +18.8pp / OCRBench +28.5pp，均 ≥5σ）、GQA tie（+1.0pp；@12.5% exact 0.505==0.505）、vz-style≡post 字节一致——与 Qwen3-VL 同构，goal 条件①（双模型结论一致）subset 尺度达成** | 诚实 nuance 入稿：Qwen2.5 post 在 DocVQA@25% 比 Qwen3 稳健（0.499 vs 0.200），差距 @12.5% 才全展开（lossy-merger 失真随压缩单调、跨代同向不同速率）；Qwen3 GQA n200 post 弱领先 4.5pp（~1.3σ，n 依赖噪声，只报方向）。stage law 维持"pre 弱占优、GQA 平手级、无 crossover"。| GPU ~50min（23 cell 串行+rescore）在 charter 范围；claim 加固未推翻。| digest `experiments/j2_crossgen_matrix.md`；J4 等价性 gate 即发（GPU 空闲）。| —

## 2026-07-24 | J4 baseline 探针 + claim 纪律调整（重要发现）| **FastV（layer-2 注意力一次性剪）同模型同预算胜 plain RBM：TextVQA 官方 0.680 vs 0.598（+8.2pp）、GQA 0.490 vs 0.420（+7pp）；但 OCRBench 崩到 post 水平 0.155（RBM 0.580，+42.5pp）——query-conditioned 排序赢回 scene-text/object，救不回 merger 已毁的 dense OCR** | 解读=layer-2 注意力天然 query-conditioned（已混问题 token）=oracle +8.2pp query 余量的实证捕获；**spine 未推翻**（post-merger 族 text-dense 全线崩 0.215/0.200/0.165 保持；pre 鲁棒+OCR 不可替代保持），但**红线升级：不得写"RBM 超过现有方法"**；可写鲁棒性（不崩、从不输 post 族）+ text-dense/OCR 大胜。Pyramid canonical（keep 62.5%，2.1× 我方预算）TextVQA 0.852 近无损→入稿作曲线点（补 pre r=0.375 同点）。**J5 QA gate 意义升级**：若 QA-pre 拿回 TextVQA/GQA 且守 OCR=双赢方法 claim（预注册判据裁决）；否则 RBM=鲁棒默认、QA=bounded negative。HF harness 验证链完整（r=0 锚 8/8、HF-vs-vLLM 16/16 等价、transformers-4.57.6 裸 tensor API 修复）。| 缺陷待补：DocVQA HF cell 两法皆 skip 187（降 max-pixels 重跑）、pyramid iso-25% 试 [1,0,0,0] 退化 schedule。| digest `experiments/j4_probe_qwen3vl.md`；J5 gate 执行中。| —

## 2026-07-24 | J5 有效 gate = NO-GO：方法冻结 plain RBM（预注册止损兑现）| **qsim 实现 bug 修复后有效测量：dev λ 选择所有 λ>0 皆负（λ0.3 −1.7pp / λ0.5 −5.2pp / λ0.7 −3.3pp，dev=textvqa+docvqa 各 32 条不相交 slice、官方指标）→ 按预注册选 λ=0 → NO-GO。方法永久冻结 = plain RBM（L2 pre-merger ranking），不再搜索任何 hybrid/router/query-aware 变体** | 第一次 NO-GO 系 qsim 从未启用的空测量（embed 查找命中 visual.pos_embed——vLLM 词嵌入是 VocabParallelEmbedding 非 nn.Embedding），修复后（c92e32f，embed=language_model.model.embed_tokens 151936、blend 真启用）重测得真负结果。**机制强化**：pre-merger 特征纯视觉无 query 信息，query 依赖只能在 LLM 交叉注意力后获得=FastV layer-2 胜 TextVQA/GQA 的机制解释（J4 实证）+ oracle +8.2pp 不可达的完整证据链（图像级 07-21 / merger-aware 07-23 / query 级本次三连负）。**claim 终态**：RBM=鲁棒默认（不崩+text-dense 大胜 post 族）；FastV=query-conditioned 竞争者（胜 TextVQA/GQA、OCR 崩）；不写"超过现有方法"。GPU ~0.8h ≤2h 止损。| digest `experiments/j5_qa_gate_result.md`。下一步：J4 docvqa 同像素补齐（600k cap 五法公平）+ pyramid iso[1,0,0,0] 试 → J3 机制跨代复制 → J7 完整 split 主表。| —
